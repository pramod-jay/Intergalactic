import { StyleSheet } from 'react-native';

//External style sheet for comman use

const ExtStyles = StyleSheet.create({
    body: {
        width: '100%',
        height: '100%',
        backgroundColor: '#1C1A3A'
    },  
});

export default ExtStyles;